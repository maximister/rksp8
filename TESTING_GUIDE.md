# 🧪 Руководство по тестированию системы

## Цель
Убедиться, что вся микросервисная архитектура работает корректно.

---

## 📋 Чек-лист проверки

### Этап 1: Запуск системы ✅

- [ ] Все JAR файлы собраны
- [ ] Docker образы созданы
- [ ] Все контейнеры запущены
- [ ] Контейнеры здоровы (healthy)

### Этап 2: Проверка инфраструктуры 🏗️

- [ ] Eureka Server доступен
- [ ] Все сервисы зарегистрированы в Eureka
- [ ] Config Server работает
- [ ] Config Server отдает конфигурации

### Этап 3: Проверка безопасности 🔒

- [ ] OAuth2 сервер доступен
- [ ] Можно получить access token
- [ ] Токен валидный (JWT)
- [ ] Запросы без токена отклоняются

### Этап 4: Проверка бизнес-логики 💼

- [ ] Parking Service: CRUD операции
- [ ] Vehicle Service: CRUD операции
- [ ] Reservation Service: CRUD операции
- [ ] Reservation Service: Интеграция с другими сервисами (Feign)

### Этап 5: Проверка API Gateway 🚪

- [ ] Gateway доступен
- [ ] Роутинг работает
- [ ] Авторизация через Gateway работает

---

## 🚀 Пошаговая инструкция

### Шаг 1: Подготовка

```bash
# Перейдите в директорию проекта
cd /Users/maximister/dev/IdeaProjects/rksp8

# Убедитесь что JAR файлы собраны
ls -lh */target/*.jar | grep -v original
```

**Ожидаемый результат:** 7 JAR файлов

---

### Шаг 2: Запуск Docker

```bash
# Быстрый метод (рекомендуется)
docker-compose -f docker-compose-fast.yml build
docker-compose -f docker-compose-fast.yml up -d

# ИЛИ автоматический скрипт
./docker-build-fast.sh
```

**Ожидаемое время:** 30-60 секунд

---

### Шаг 3: Проверка статуса контейнеров

```bash
# Проверить что все контейнеры запущены
docker-compose -f docker-compose-fast.yml ps
```

**Ожидаемый результат:** 7 контейнеров со статусом `Up`

```bash
# Подождать пока все сервисы станут healthy
watch -n 2 'docker-compose -f docker-compose-fast.yml ps'
```

**Ожидаемое время:** 2-3 минуты для полной инициализации

---

### Шаг 4: Проверка Eureka Dashboard

```bash
# Открыть в браузере
open http://localhost:8761
```

**Ожидаемый результат:** 
- Должны быть видны 6 зарегистрированных сервисов:
  - CONFIG-SERVER
  - AUTH-SERVER
  - API-GATEWAY
  - PARKING-SERVICE
  - VEHICLE-SERVICE
  - RESERVATION-SERVICE

📸 **Скриншот:** Сохраните для отчета!

---

### Шаг 5: Проверка Config Server

```bash
# Проверить что Config Server работает
curl -s http://localhost:8888/actuator/health | jq

# Получить конфигурацию для parking-service
curl -s http://localhost:8888/parking-service/default | jq '.propertySources[0].source' | head -20
```

**Ожидаемый результат:**
```json
{
  "status": "UP"
}
```

---

### Шаг 6: Получение OAuth2 токена

```bash
# Получить access token
curl -X POST http://localhost:9000/oauth2/token \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "grant_type=client_credentials&client_id=client&client_secret=secret" \
  | jq

# Сохранить токен в переменную
export TOKEN=$(curl -s -X POST http://localhost:9000/oauth2/token \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "grant_type=client_credentials&client_id=client&client_secret=secret" \
  | jq -r '.access_token')

echo "Token: $TOKEN"
```

**Ожидаемый результат:**
```json
{
  "access_token": "eyJraWQiOiI...",
  "token_type": "Bearer",
  "expires_in": 300
}
```

---

### Шаг 7: Тестирование API без токена (должно упасть)

```bash
# Попытка без токена - должна быть ошибка 401
curl -i http://localhost:8090/parking/api/spots
```

**Ожидаемый результат:** HTTP 401 Unauthorized

---

### Шаг 8: Тестирование Parking Service

```bash
# Создать парковочное место
curl -X POST http://localhost:8090/parking/api/spots \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "spotNumber": "A-101",
    "status": "AVAILABLE"
  }' | jq

# Получить все парковочные места
curl -X GET http://localhost:8090/parking/api/spots \
  -H "Authorization: Bearer $TOKEN" | jq

# Получить конкретное место (ID=1)
curl -X GET http://localhost:8090/parking/api/spots/1 \
  -H "Authorization: Bearer $TOKEN" | jq

# Обновить место
curl -X PUT http://localhost:8090/parking/api/spots/1 \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "spotNumber": "A-101",
    "status": "OCCUPIED"
  }' | jq
```

**Ожидаемый результат:** Успешные ответы с данными

---

### Шаг 9: Тестирование Vehicle Service

```bash
# Создать транспортное средство
curl -X POST http://localhost:8090/vehicles/api/vehicles \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "licensePlate": "А123БВ777",
    "model": "Toyota Camry",
    "ownerName": "Иван Иванов"
  }' | jq

# Получить все транспортные средства
curl -X GET http://localhost:8090/vehicles/api/vehicles \
  -H "Authorization: Bearer $TOKEN" | jq

# Получить по ID
curl -X GET http://localhost:8090/vehicles/api/vehicles/1 \
  -H "Authorization: Bearer $TOKEN" | jq
```

**Ожидаемый результат:** Успешные ответы с данными

---

### Шаг 10: Тестирование Reservation Service (с интеграцией)

```bash
# Создать бронирование (проверяет интеграцию через Feign)
curl -X POST http://localhost:8090/reservations/api/reservations \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "parkingSpotId": 1,
    "vehicleId": 1,
    "startTime": "2024-12-01T10:00:00",
    "endTime": "2024-12-01T18:00:00"
  }' | jq

# Получить детали бронирования (проверяет Feign вызовы)
curl -X GET http://localhost:8090/reservations/api/reservations/1/details \
  -H "Authorization: Bearer $TOKEN" | jq

# Получить все бронирования
curl -X GET http://localhost:8090/reservations/api/reservations \
  -H "Authorization: Bearer $TOKEN" | jq
```

**Ожидаемый результат:** 
- Бронирование создано
- Details содержат данные из Parking и Vehicle сервисов
- Статус парковочного места изменен на OCCUPIED

---

### Шаг 11: Проверка логов

```bash
# Посмотреть логи всех сервисов
docker-compose -f docker-compose-fast.yml logs --tail=50

# Логи конкретного сервиса
docker-compose -f docker-compose-fast.yml logs -f reservation-service
```

**Что искать:**
- ✅ Нет ERROR или WARN сообщений
- ✅ Успешная регистрация в Eureka
- ✅ Успешное получение конфигурации из Config Server
- ✅ Успешные Feign вызовы

---

## 🎯 Критерии успеха

### Минимальные требования (ОБЯЗАТЕЛЬНО):

1. ✅ Все 7 контейнеров запущены
2. ✅ Все 6 сервисов видны в Eureka
3. ✅ Можно получить OAuth2 токен
4. ✅ CRUD операции работают на всех 3 бизнес-сервисах
5. ✅ Запросы без токена отклоняются (401)

### Дополнительные проверки (ЖЕЛАТЕЛЬНО):

6. ✅ Feign интеграция работает (Reservation → Parking, Vehicle)
7. ✅ Config Server отдает конфигурации
8. ✅ API Gateway корректно роутит запросы
9. ✅ Load Balancing работает (если запущено несколько инстансов)

---

## 🐛 Что делать если что-то не работает

### Проблема: Контейнер не запускается

```bash
# Посмотреть логи
docker-compose -f docker-compose-fast.yml logs <service-name>

# Перезапустить конкретный сервис
docker-compose -f docker-compose-fast.yml restart <service-name>
```

### Проблема: Сервис не регистрируется в Eureka

```bash
# Проверить что Eureka доступен из контейнера
docker-compose -f docker-compose-fast.yml exec <service-name> curl http://eureka-server:8761/actuator/health

# Подождать 1-2 минуты - регистрация может занять время
```

### Проблема: 401 даже с токеном

```bash
# Проверить что токен валиден
echo $TOKEN | cut -d. -f2 | base64 -d | jq

# Получить новый токен
export TOKEN=$(curl -s -X POST http://localhost:9000/oauth2/token \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "grant_type=client_credentials&client_id=client&client_secret=secret" \
  | jq -r '.access_token')
```

### Проблема: Feign вызовы не работают

```bash
# Проверить что целевые сервисы зарегистрированы в Eureka
curl http://localhost:8761/eureka/apps | grep -A5 PARKING-SERVICE

# Проверить логи сервиса
docker-compose -f docker-compose-fast.yml logs reservation-service | grep -i feign
```

---

## 📊 Отчет о тестировании

После завершения тестирования заполните:

```
Дата тестирования: ___________
Версия: 1.0-SNAPSHOT

Результаты:
[ ] Все контейнеры запущены
[ ] Eureka показывает 6 сервисов
[ ] OAuth2 токен получен
[ ] Parking Service: CRUD работает
[ ] Vehicle Service: CRUD работает  
[ ] Reservation Service: CRUD работает
[ ] Feign интеграция работает
[ ] API Gateway роутинг работает

Найденные проблемы: ___________

Скриншоты:
- Eureka Dashboard: ___________
- Успешный API запрос: ___________
```

---

## 🎓 Для демонстрации преподавателю

Подготовьте:

1. **Eureka Dashboard** - показать зарегистрированные сервисы
2. **Postman Collection** или **curl команды** - показать работающие API
3. **Логи** - показать что нет ошибок
4. **Архитектурная диаграмма** - объяснить взаимодействие сервисов

Время демо: ~10 минут

---

## ✨ Бонус: Автоматический тест-скрипт

См. файл `test-all.sh` для автоматизированного тестирования всех endpoints.

