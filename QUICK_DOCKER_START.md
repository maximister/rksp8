# 🚀 Быстрый запуск через Docker

Этот метод в **10-20 раз быстрее** стандартной сборки, потому что использует локально собранные JAR файлы.

## Способ 1: Автоматический (Рекомендуется)

```bash
./docker-build-fast.sh
```

Скрипт сделает всё сам, просто следуйте инструкциям.

## Способ 2: Ручной (если нужен полный контроль)

### Шаг 1: Соберите JAR файлы в IntelliJ IDEA

**Вариант A: Через UI**
1. Откройте проект в IntelliJ IDEA
2. Справа найдите вкладку "Maven"
3. Раскройте `rksp8` → `Lifecycle`
4. Дважды кликните на `clean`
5. Дважды кликните на `package`
6. Дождитесь завершения (1-2 минуты)

**Вариант B: Через терминал IDEA**
```bash
mvn clean package -DskipTests
```

### Шаг 2: Соберите Docker образы (быстро!)

```bash
docker-compose -f docker-compose-fast.yml build
```

Это займет **30-60 секунд** вместо 20-30 минут!

### Шаг 3: Запустите контейнеры

```bash
docker-compose -f docker-compose-fast.yml up -d
```

### Проверка статуса

```bash
docker-compose -f docker-compose-fast.yml ps
```

### Просмотр логов

```bash
# Все сервисы
docker-compose -f docker-compose-fast.yml logs -f

# Конкретный сервис
docker-compose -f docker-compose-fast.yml logs -f eureka-server
```

### Остановка

```bash
docker-compose -f docker-compose-fast.yml down
```

## Почему это быстро?

| Метод | Время сборки | Причина |
|-------|--------------|---------|
| **Стандартный** (`docker-compose build`) | 20-30 минут | Каждый сервис загружает ~200MB зависимостей из интернета |
| **Быстрый** (`docker-compose-fast.yml`) | 30-60 секунд | Использует локальный Maven кэш IDEA, только копирует готовые JAR |

## Обновление кода

Если вы изменили код:

1. Пересоберите JAR в IDEA (Maven → package)
2. Пересоберите Docker образы:
   ```bash
   docker-compose -f docker-compose-fast.yml build
   docker-compose -f docker-compose-fast.yml up -d
   ```

## Доступ к сервисам

После запуска:

- **Eureka Dashboard**: http://localhost:8761
- **Config Server**: http://localhost:8888
- **Auth Server**: http://localhost:9000
- **API Gateway**: http://localhost:8090
- **Parking Service**: http://localhost:8081
- **Vehicle Service**: http://localhost:8082
- **Reservation Service**: http://localhost:8083

## Troubleshooting

**Проблема**: JAR файлы не найдены

**Решение**: Убедитесь, что собрали проект в IDEA. Проверьте наличие файлов:
```bash
ls -lh */target/*.jar
```

**Проблема**: Порт уже занят

**Решение**: Измените порты в `docker-compose-fast.yml` или остановите конфликтующие сервисы.

