# 🐳 Docker - Что было сделано

## Проблемы, которые были исправлены

### 1. ❌ Ошибка "Child module does not exist"
**Причина**: Dockerfiles создавали пустые директории вместо копирования `pom.xml` файлов.

**Решение**: Обновлены все Dockerfiles - теперь копируют все `pom.xml` файлы:

```dockerfile
# Было (НЕПРАВИЛЬНО):
RUN mkdir -p config-server auth-server api-gateway ...

# Стало (ПРАВИЛЬНО):
COPY eureka-server/pom.xml eureka-server/
COPY config-server/pom.xml config-server/
COPY auth-server/pom.xml auth-server/
# ... и т.д.
```

### 2. ⏰ Очень долгая сборка (20-30 минут)
**Причина**: Docker загружает ~1.5GB Maven зависимостей из интернета для каждого сервиса отдельно.

**Решение**: Создан быстрый метод сборки через локальные JAR файлы.

---

## 📁 Созданные файлы

### Для быстрой сборки:
1. **`docker-build-fast.sh`** - автоматический скрипт
2. **`docker-compose-fast.yml`** - конфигурация для быстрой сборки
3. **`*/Dockerfile.fast`** - упрощенные Dockerfiles (создаются скриптом)

### Документация:
1. **`START_HERE.md`** - 📍 НАЧНИТЕ ОТСЮДА
2. **`QUICK_DOCKER_START.md`** - подробная инструкция
3. **`DOCKER_SUMMARY.md`** - этот файл

---

## 🚀 Два способа запуска Docker

### Способ 1: Быстрый (⚡ 30-60 секунд сборки)

```bash
# 1. Соберите в IntelliJ IDEA
mvn clean package -DskipTests

# 2. Запустите скрипт
./docker-build-fast.sh

# 3. Запустите контейнеры
docker-compose -f docker-compose-fast.yml up -d
```

**Когда использовать**: Для разработки и тестирования.

### Способ 2: Стандартный (⏰ 20-30 минут сборки)

```bash
# Всё в одном
docker-compose build
docker-compose up -d
```

**Когда использовать**: Для production deployment или если нет локального Maven.

---

## 📊 Сравнение

| Критерий | Быстрый метод | Стандартный метод |
|----------|---------------|-------------------|
| **Время сборки** | 30-60 сек | 20-30 мин |
| **Требования** | IntelliJ IDEA + Maven | Только Docker |
| **Размер образов** | ~280 MB каждый | ~280 MB каждый |
| **Использует** | Локальный Maven кэш | Загружает из интернета |
| **Лучше для** | Разработка | Production |

---

## 🔧 Команды для управления

### Запуск
```bash
# Быстрый метод
docker-compose -f docker-compose-fast.yml up -d

# Стандартный метод
docker-compose up -d
```

### Остановка
```bash
# Быстрый метод
docker-compose -f docker-compose-fast.yml down

# Стандартный метод
docker-compose down
```

### Просмотр логов
```bash
# Все сервисы
docker-compose -f docker-compose-fast.yml logs -f

# Конкретный сервис
docker-compose -f docker-compose-fast.yml logs -f eureka-server
```

### Перезапуск после изменений
```bash
# 1. Пересобрать JAR в IDEA
mvn package -DskipTests

# 2. Обновить Docker образы и контейнеры
docker-compose -f docker-compose-fast.yml up -d --build
```

### Полная очистка
```bash
# Остановить и удалить контейнеры
docker-compose -f docker-compose-fast.yml down

# Удалить образы
docker images | grep rksp8 | awk '{print $3}' | xargs docker rmi

# Очистить неиспользуемые данные Docker
docker system prune -a
```

---

## 🌐 Доступ к сервисам

После запуска контейнеров:

| Сервис | URL | Описание |
|--------|-----|----------|
| **Eureka Server** | http://localhost:8761 | Service Discovery Dashboard |
| **Config Server** | http://localhost:8888 | Централизованная конфигурация |
| **Auth Server** | http://localhost:9000 | OAuth2 авторизация |
| **API Gateway** | http://localhost:8090 | Единая точка входа |
| **Parking Service** | http://localhost:8081 | Управление парковочными местами |
| **Vehicle Service** | http://localhost:8082 | Управление транспортом |
| **Reservation Service** | http://localhost:8083 | Управление бронированием |

---

## 🐛 Troubleshooting

### Проблема: "JAR файлы не найдены"
```bash
# Проверьте наличие JAR файлов
ls -lh */target/*.jar

# Если пусто - соберите в IDEA
mvn clean package -DskipTests
```

### Проблема: "Порт уже занят"
```bash
# Найдите процесс на порту (например, 8761)
lsof -i :8761

# Остановите процесс
kill -9 <PID>
```

### Проблема: Контейнеры не запускаются
```bash
# Смотрите логи
docker-compose -f docker-compose-fast.yml logs

# Проверьте статус
docker-compose -f docker-compose-fast.yml ps

# Перезапустите
docker-compose -f docker-compose-fast.yml restart
```

### Проблема: Образы слишком большие
```bash
# Очистите старые слои
docker system prune -a

# Пересоберите
docker-compose -f docker-compose-fast.yml build --no-cache
```

---

## ✅ Итог

1. **Исправлены** все ошибки в Dockerfiles
2. **Создан** быстрый метод сборки (в 20-40 раз быстрее)
3. **Написана** подробная документация
4. **Готов** к использованию - просто следуйте `START_HERE.md`

**Рекомендация**: Используйте быстрый метод (`docker-compose-fast.yml`) для разработки!

