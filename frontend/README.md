# 🅿️ Фронтенд для системы управления парковкой

Простой и красивый веб-интерфейс для демонстрации работы микросервисной системы управления парковкой.

## 🚀 Быстрый старт

### 1. Запустите все микросервисы

Убедитесь, что все сервисы запущены в IntelliJ IDEA:
- ✅ Eureka Server (порт 8761)
- ✅ Config Server (порт 8888)
- ✅ Auth Server (порт 9000)
- ✅ API Gateway (порт 8090)
- ✅ Parking Service (порт 8081)
- ✅ Vehicle Service (порт 8082)
- ✅ Reservation Service (порт 8083)

### 2. Откройте фронтенд

Просто откройте файл `index.html` в браузере:

```bash
# Из корня проекта
open frontend/index.html

# Или используйте простой HTTP сервер (опционально)
cd frontend
python3 -m http.server 8000
# Затем откройте http://localhost:8000
```

### 3. Авторизуйтесь

Выберите одну из ролей:

**🔑 Администратор** (полный доступ):
- Может создавать, просматривать и удалять данные
- Scope: `read write`

**👤 Пользователь** (только чтение):
- Может только просматривать данные
- Scope: `read`

## 📋 Функциональность

### Парковочные места
- ✅ Просмотр списка всех мест
- ✅ Добавление новых мест (только администратор)
- ✅ Удаление мест (только администратор)
- ✅ Автоматическое обновление статуса при бронировании

### Автомобили
- ✅ Просмотр всех зарегистрированных автомобилей
- ✅ Регистрация новых автомобилей (только администратор)
- ✅ Удаление автомобилей (только администратор)

### Бронирования
- ✅ Просмотр всех бронирований
- ✅ Создание новых бронирований (только администратор)
- ✅ Отмена бронирований (только администратор)
- ✅ Автоматическое изменение статуса парковочного места

## 🎨 Возможности интерфейса

- 🎯 **Современный дизайн** с градиентным фоном
- 📱 **Адаптивная верстка** для мобильных устройств
- 🔔 **Toast-уведомления** о результатах операций
- 🔐 **Ролевой доступ** (администратор/пользователь)
- ⚡ **Мгновенное обновление** данных после операций
- 🎭 **Статусные бэйджи** с цветовой индикацией

## 🔧 Технические детали

### Архитектура запросов

```
Frontend (JavaScript)
    ↓
    ├─→ OAuth2 Token: http://localhost:9000/oauth2/token (прямой запрос к Auth Server)
    │
    └─→ Business API: http://localhost:8090/api/* (через API Gateway)
            ↓
            ├─→ /api/spots → Parking Service
            ├─→ /api/vehicles → Vehicle Service
            └─→ /api/reservations → Reservation Service
```

### Конфигурация (в `app.js`)

```javascript
const API_BASE_URL = 'http://localhost:8090/api';
const AUTH_URL = 'http://localhost:9000/oauth2/token';
const CLIENT_ID = 'parking-system-client';
const CLIENT_SECRET = 'secret';
```

### CORS (Cross-Origin Resource Sharing)

Если вы открываете `index.html` напрямую (протокол `file://`), браузер может блокировать CORS-запросы.

**Решение**: Используйте простой HTTP-сервер:

```bash
# Python 3
cd frontend
python3 -m http.server 8000

# Node.js (если установлен)
npx http-server -p 8000

# PHP (если установлен)
php -S localhost:8000
```

Затем откройте: `http://localhost:8000`

## 🎬 Демонстрация для преподавателя

### Сценарий 1: Создание полного цикла бронирования

1. Войдите как **Администратор**
2. Перейдите на вкладку **"Парковочные места"**
3. Нажмите **"+ Добавить место"**
   - Номер: `A-101`
   - Этаж: `1`
   - Статус: `Свободно`
4. Перейдите на вкладку **"Автомобили"**
5. Нажмите **"+ Добавить автомобиль"**
   - Гос. номер: `A123BC77`
   - Модель: `Toyota Camry`
   - Цвет: `Черный`
   - Владелец: `Иван Иванов`
6. Перейдите на вкладку **"Бронирования"**
7. Нажмите **"+ Создать бронирование"**
   - Выберите созданное место и автомобиль
   - Установите время начала и окончания
8. **Проверьте**: вернитесь на вкладку "Парковочные места" — статус должен измениться на **"Занято"**

### Сценарий 2: Проверка прав доступа

1. Войдите как **Пользователь**
2. Убедитесь, что кнопки **"+ Добавить..."** **скрыты**
3. Можно просматривать все данные, но нельзя изменять

### Сценарий 3: Отмена бронирования

1. Войдите как **Администратор**
2. Перейдите на вкладку **"Бронирования"**
3. Нажмите **"❌ Отменить"** на активном бронировании
4. **Проверьте**: статус парковочного места вернулся в **"Свободно"**

## 🐛 Troubleshooting

### Проблема: "Ошибка авторизации"
**Решение**: Убедитесь, что Auth Server запущен на порту 9000

### Проблема: "Ошибка загрузки данных"
**Решение**: 
1. Проверьте, что API Gateway запущен на порту 8090
2. Проверьте консоль браузера (F12) для детальной информации об ошибке

### Проблема: CORS-ошибки
**Решение**: Используйте HTTP-сервер вместо прямого открытия файла

### Проблема: "Недостаточно прав доступа"
**Решение**: Вы вошли как Пользователь, но пытаетесь выполнить операцию записи. Войдите как Администратор.

## 📦 Файлы проекта

```
frontend/
├── index.html    # Главная HTML-страница
├── styles.css    # Стили и дизайн
├── app.js        # Логика приложения (API, state management)
└── README.md     # Эта документация
```

## 🎓 Для защиты ДЗ

Этот фронтенд демонстрирует:

1. ✅ **Микросервисную архитектуру** — запросы идут через API Gateway
2. ✅ **OAuth2 авторизацию** — Client Credentials Grant
3. ✅ **Ролевой доступ** — разные scopes для администратора и пользователя
4. ✅ **Service Discovery** — Gateway автоматически находит сервисы через Eureka
5. ✅ **Взаимодействие сервисов** — Reservation Service вызывает Parking Service через Feign
6. ✅ **Централизованную конфигурацию** — все настройки из Config Server

---

**Приятной демонстрации! 🚀**

