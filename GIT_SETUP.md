# Инструкция по настройке Git репозитория для Config Server

## 📋 Что нужно сделать

### Вариант 1: Использование локального режима (РЕКОМЕНДУЕТСЯ для разработки)

Config Server уже настроен на работу в **native режиме** - он читает конфиги из папки `config-repo` в проекте.

**Ничего дополнительно делать НЕ нужно!** Просто закоммитьте изменения:

```bash
cd /Users/maximister/dev/IdeaProjects/rksp8

# Добавить все новые файлы
git add .

# Проверить что добавилось
git status

# Закоммитить
git commit -m "Add Spring Cloud Config Server with centralized configuration"

# Запушить (если есть удаленный репозиторий)
git push origin main
```

### Вариант 2: Использование Git репозитория (для продакшена)

Если хотите, чтобы Config Server читал конфиги из GitHub:

#### Шаг 1: Убедитесь, что проект залит на GitHub

```bash
# Если еще не создали репозиторий на GitHub:
# 1. Зайдите на https://github.com/new
# 2. Создайте новый репозиторий (например: rksp8)
# 3. НЕ инициализируйте его с README

# Если это новый проект:
cd /Users/maximister/dev/IdeaProjects/rksp8
git init
git add .
git commit -m "Initial commit with Config Server"
git branch -M main
git remote add origin https://github.com/your-username/rksp8.git
git push -u origin main

# Если проект уже существует:
git add .
git commit -m "Add Spring Cloud Config Server"
git push origin main
```

#### Шаг 2: Переключите Config Server на Git режим

Отредактируйте `config-server/src/main/resources/application.yml`:

```yaml
spring:
  profiles:
    active: git  # Измените с 'native' на 'git'
  cloud:
    config:
      server:
        git:
          uri: https://github.com/your-username/rksp8  # Ваш репозиторий!
          search-paths: config-repo
          clone-on-start: true
          default-label: main
          # Для приватного репозитория добавьте:
          # username: your-username
          # password: ghp_your_personal_access_token
```

#### Шаг 3: Создайте Personal Access Token (для приватного репо)

Если репозиторий приватный:

1. Зайдите на https://github.com/settings/tokens
2. Нажмите "Generate new token (classic)"
3. Выберите scope: `repo` (Full control of private repositories)
4. Скопируйте токен
5. Добавьте в `application.yml`:

```yaml
git:
  uri: https://github.com/your-username/rksp8
  username: your-username
  password: ghp_xxxxxxxxxxxxxxxxxxxxxxxxx  # Ваш токен
  search-paths: config-repo
```

## 🔐 Безопасность (НЕ храните токены в коде!)

Для продакшена используйте переменные окружения:

```yaml
git:
  uri: ${GIT_REPO_URI:https://github.com/your-username/rksp8}
  username: ${GIT_USERNAME:}
  password: ${GIT_PASSWORD:}
  search-paths: config-repo
```

И запускайте с переменными:

```bash
export GIT_REPO_URI=https://github.com/your-username/rksp8
export GIT_USERNAME=your-username
export GIT_PASSWORD=ghp_your_token
mvn spring-boot:run
```

## 📝 Проверка настроек

### После коммита в Git:

```bash
# 1. Проверьте, что конфиги в репозитории
git log --oneline
git ls-files config-repo/

# 2. Перезапустите Config Server
cd config-server
mvn spring-boot:run

# 3. Проверьте, что конфиги доступны
curl http://localhost:8888/parking-service/default
curl http://localhost:8888/vehicle-service/default
```

## 🎯 Какой режим использовать?

| Режим | Когда использовать | Преимущества |
|-------|-------------------|--------------|
| **Native** | Разработка, учебные проекты | Проще настроить, быстрее работает |
| **Git** | Продакшен, командная работа | Версионность, история изменений, можно откатить |

## 📂 Структура для демонстрации преподавателю

Для учебного проекта **рекомендуется Native режим**, потому что:

✅ Проще демонстрировать - все в одном проекте  
✅ Не нужно создавать отдельный репозиторий  
✅ Не нужны токены доступа  
✅ Преподаватель видит все конфиги в вашем проекте  

Но в README укажите, что **Config Server поддерживает оба режима**:

```markdown
## Spring Cloud Config Server

Config Server настроен на работу с Git в качестве источника данных.

Текущий режим: **Native** (для удобства разработки)
- Конфиги хранятся в папке `config-repo/`
- Config Server читает их из локальной файловой системы

Поддерживается режим: **Git** 
- Можно переключить на чтение из Git репозитория
- Настройки в `config-server/application.yml` (профиль 'git')
```

## 🚀 Готовые команды

```bash
# 1. Закоммитить изменения
git add .
git commit -m "Add Spring Cloud Config Server"

# 2. Запушить (если есть удаленный репозиторий)
git push origin main

# 3. Запустить Config Server
cd config-server
mvn spring-boot:run

# 4. Проверить
curl http://localhost:8888/actuator/health
curl http://localhost:8888/parking-service/default
```

Готово! 🎉

