# ⚡ БЫСТРЫЙ СТАРТ

## Проблема
Стандартный `docker-compose build` занимает **20-30 минут** ⏰

## Решение
Используйте быстрый метод - **30-60 секунд** ⚡

---

## 📋 Инструкция (3 простых шага)

### 1️⃣ Соберите проект в IntelliJ IDEA

Откройте **терминал внутри IntelliJ IDEA** и выполните:

```bash
mvn clean package -DskipTests
```

Подождите 2-3 минуты пока соберутся все модули.

### 2️⃣ Запустите автоматический скрипт

В **обычном терминале** (не в IDEA):

```bash
cd /Users/maximister/dev/IdeaProjects/rksp8
./docker-build-fast.sh
```

Когда скрипт спросит "Нажмите Enter после сборки в IDEA" - просто нажмите Enter (вы уже собрали в шаге 1).

### 3️⃣ Запустите контейнеры

```bash
docker-compose -f docker-compose-fast.yml up -d
```

---

## ✅ Проверка

```bash
# Статус контейнеров
docker-compose -f docker-compose-fast.yml ps

# Логи
docker-compose -f docker-compose-fast.yml logs -f

# Eureka Dashboard
open http://localhost:8761
```

---

## 🛑 Остановка

```bash
docker-compose -f docker-compose-fast.yml down
```

---

## 💡 Почему это быстрее?

- **Медленный способ**: Docker загружает ~1.5GB зависимостей из интернета 7 раз (по разу для каждого сервиса)
- **Быстрый способ**: IntelliJ загружает зависимости 1 раз и кэширует, Docker только копирует готовые JAR файлы

---

## 🔄 Если изменили код

1. Пересоберите в IDEA: `mvn package -DskipTests`
2. Обновите Docker: `docker-compose -f docker-compose-fast.yml up -d --build`

---

## ❓ Проблемы?

**"JAR файлы не найдены"**
→ Убедитесь, что запустили `mvn clean package -DskipTests` в терминале IDEA

**"Порт уже занят"**
→ Остановите другие сервисы на портах 8761, 8888, 9000, 8080-8084, 8090

**Нужна помощь?**
→ Смотрите подробную инструкцию в `QUICK_DOCKER_START.md`

